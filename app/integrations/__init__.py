"""Implementações reais (OpenCV/MediaPipe/Faster-Whisper/ffmpeg/Ollama).

Exigem o extra `ml` (`uv sync --extra ml`) e hardware/modelos; são acionadas apenas com
``APP_USE_REAL_PIPELINE=true`` (estágio final de aprovação). Ficam fora da cobertura de CI.
"""
